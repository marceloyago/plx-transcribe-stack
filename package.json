{
  "name": "plx-transcribe-stack",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PlayLoadX — stack de transcrição (contratos TS + API Python real)",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx/esm --test contracts/srt.test.ts contracts/plxTranscribeClient.test.ts",
    "test:py": "cd transcribe-service && python -m pytest -q"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
